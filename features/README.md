// Example modularization structure for future feature modules
// Place feature-specific code in these modules for better build speed and organization

// To convert to real Gradle modules, move code and add build.gradle.kts for each

features/
  onboarding/
  mining/
  wallet/
  analytics/
  settings/
